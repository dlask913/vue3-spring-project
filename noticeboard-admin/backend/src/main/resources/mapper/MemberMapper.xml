<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.limnj.noticeboardadmin.member.MemberMapper">
    <resultMap id="AdminMemberResultMap" type="AdminMemberResponseDto">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email"
                column="email"
                typeHandler="com.limnj.noticeboardadmin.config.handler.EncryptedStringTypeHandler"/>
        <result property="role" column="user_role"/>
        <result property="loginFailCount" column="login_fail_count"/>
        <result property="lockUntil" column="lock_until"/>
    </resultMap>

    <!-- 관리자 회원 가입 -->
    <insert id="saveAdminMember" parameterType="AdminMemberRequestDto" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO members (username, password, email, user_type)
        VALUES (#{username}, #{password}, #{email, typeHandler=com.limnj.noticeboardadmin.config.handler.EncryptedStringTypeHandler}, 'ADMIN')
    </insert>

    <!-- 관리자 회원 단일 조회 by PK -->
    <select id="findMemberById" parameterType="long" resultMap="AdminMemberResultMap">
        SELECT id, username, password, email, user_role
        FROM members
        WHERE id = #{id} AND user_type = 'ADMIN'
    </select>

    <!-- 관리자 회원 단일 조회 by Username -->
    <select id="findMemberByUsername" parameterType="String" resultMap="AdminMemberResultMap">
        SELECT id, username, password, email, user_role, login_fail_count, lock_until
        FROM members
        WHERE username = #{username} AND user_type = 'ADMIN'
    </select>

    <!-- 특정 이메일을 갖는 회원 존재 여부 확인 -->
    <select id="existsByEmail" parameterType="String" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM members
            WHERE email = #{email, typeHandler=com.limnj.noticeboardadmin.config.handler.EncryptedStringTypeHandler} AND user_type = 'ADMIN'
        )
    </select>

    <!-- 회원 2FA QR secretKey 저장 -->
    <update id ="updateSecretKeyByEmail">
        UPDATE members
        SET secret_key = #{secretKey}, updated_at = NOW()
        WHERE email = #{email, typeHandler=com.limnj.noticeboardadmin.config.handler.EncryptedStringTypeHandler} AND user_type = 'ADMIN'
    </update>

    <!-- 회원 2FA QR secretKey 조회 -->
    <select id="findSecretKeyByEmail" parameterType="String" resultType="String">
        SELECT secret_key
        FROM members
        WHERE email = #{email, typeHandler=com.limnj.noticeboardadmin.config.handler.EncryptedStringTypeHandler} AND user_type = 'ADMIN'
    </select>

    <!-- 로그인 실패 횟수 증가 -->
    <update id="incrementFailCount" parameterType="String">
        UPDATE members
        SET login_fail_count = login_fail_count + 1,
            lock_until = CASE
                WHEN login_fail_count + 1 >= 5
                THEN DATE_ADD(NOW(), INTERVAL 5 MINUTE)
                ELSE lock_until
            END
        WHERE username = #{username}
    </update>
</mapper>